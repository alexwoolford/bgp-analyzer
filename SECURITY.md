# Security

Report vulnerabilities privately to the repository owner (GitHub Security Advisories preferred).

This project fetches public BGP and PeeringDB data over the network. Do not commit secrets, credentials, or proprietary datasets. Runtime config may use environment files under `/opt/bgp-analyzer/etc/` on deployed hosts—those stay off the public tree.
